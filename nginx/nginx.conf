# Use forwarded protocol if available, otherwise https
map $http_x_forwarded_proto $proto {
    default "https";
    http "http";
    https "https";
}

# Use forwarded host if available, otherwise original host
map $http_x_forwarded_host $forwarded_host {
    default $http_host;
    "" $http_host;
    ~^(.+)$ $1;
}

server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;

    # Allow Kubernetes probes
    if ($http_user_agent ~* "kube-probe") {
        set $skip_probe 1;
    }

    # Reject requests not from ingress (no X-Forwarded-Proto header)
    if ($http_x_forwarded_proto = "") {
        return 444;
    }

    # Redirect directory requests without trailing slash to with trailing slash
    rewrite ^([^.]*[^/])$ $proto://$forwarded_host$uri/ permanent;

    # Serve index.html for directory requests
    index index.html;

    # Serve custom error pages
    error_page 404 =404 /404.html;
    error_page 500 502 503 504 =500 /50x.html;

    # Cache busting for static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css text/xml text/javascript application/json;
}
